# Changelog

## 0.2.0 - 2026-08-06

### Added

- 暖色像素 UI、小窝分区布局和响应式聊天侧栏。
- 原创程序化兔兔与毛毛角色。
- 分层头饰、颈饰、背饰、面部贴纸。
- 小窝聊天、头顶气泡、历史消息和发送限流。
- 沙发双座、床铺、地毯的占用与状态同步。
- 洗澡泡泡、梳毛闪光、吃饭、喝水、读书和玩球效果。
- 手机方向键与互动按钮。
- 数据库外观字段增量迁移。

### Changed

- 前端从 Phaser/Vite 构建迁移到自包含 Canvas 客户端。
- Socket.IO 客户端由服务端直接提供。
- HTTP-only 部署自动按 Host 校验同源，不再写入内网 IP。
- 静态资源默认 no-cache，避免更新后旧 JS 残留。

### Fixed

- 异步表单事件中 `currentTarget` 变成 null 后调用 `reset()` 的问题。
- 源文件已改但运行目录 `dist` 未更新的问题。
- `startCareAction` 重复调用 ack 的问题。
